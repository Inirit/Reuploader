type StorageObject = browser.storage.StorageObject

class C {
    a: number
    b = 3
    method() {}
}

interface _C {
    a: number
    b: number
}

type _C2 = {a: number, b: number}

async function foo() {
    let result: StorageObject;

    /* result = await browser.storage.local.get(new Array(1, 2)) */
    /* result = await browser.storage.local.get({length: 4, func: ()=>{}}) */
    result = await browser.storage.local.get({length: 4, b: {c: "herro"}})
    result = await browser.storage.local.get(["a", "b", "c"])
    /* let c = new C() as _C */
    let c: _C2 = new C()
    result = await browser.storage.local.get(c)
    console.log(result.b)
}
